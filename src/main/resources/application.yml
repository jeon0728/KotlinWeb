spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/test
    username: sa
    password:
  jpa:
    database: H2
    show-sql: true # Jpa ?? ? SQL? ?? ??
    properties:
      hibernate:
        format_sql: true # ?? ??? ?? ????? ??
    #defer-datasource-initialization : true
server:
  port: 8081